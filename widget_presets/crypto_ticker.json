{
  "name": "crypto_ticker",
  "description": "Live crypto price ticker with 24h change",
  "default_size": [
    22,
    18
  ],
  "refresh_interval": 3600,
  "data_provider": "crypto",
  "data": {
    "symbols": [
      "bitcoin",
      "ethereum",
      "solana"
    ],
    "currency": "usd"
  },
  "html_template": "<div id='crypto-ticker'></div><script>(function(){ var d = WIDGET_DATA || {}; var tickers = d.tickers || []; var el = document.getElementById('crypto-ticker'); var html = '<div class=\"ticker-title\">CRYPTO</div>'; function parseNum(v){ if(typeof v === 'number') return Number.isFinite(v) ? v : null; if(typeof v === 'string'){ var n = Number(v.replace(/,/g, '')); return Number.isFinite(n) ? n : null; } return null; } tickers.forEach(function(t){ var change = parseNum(t.change_24h); if(change === null) change = 0; var color = change >= 0 ? 'var(--accent-alt, #44cc44)' : '#ff4444'; var arrow = change >= 0 ? '\\u25B2' : '\\u25BC'; var name = t.symbol.charAt(0).toUpperCase() + t.symbol.slice(1); var priceNum = parseNum(t.price); var price = priceNum === null ? (t.price || '--') : priceNum.toLocaleString('en-US', {minimumFractionDigits:2, maximumFractionDigits:2}); html += '<div class=\"ticker-row\"><span class=\"ticker-name\">' + name + '</span><span class=\"ticker-price\">$' + price + '</span><span class=\"ticker-change\" style=\"color:' + color + '\">' + arrow + ' ' + Math.abs(change).toFixed(2) + '%</span></div>'; }); el.innerHTML = html; })();</script>",
  "css": "#crypto-ticker { height:100%; display:flex; flex-direction:column; justify-content:center; padding:0.8em; } #crypto-ticker .ticker-title { font-size:clamp(0.6rem,3.2cqi,0.9rem); color:var(--fg-dim,#888); letter-spacing:0.2em; margin-bottom:0.5em; } #crypto-ticker .ticker-row { display:flex; justify-content:space-between; align-items:center; padding:0.3em 0; border-bottom:1px solid rgba(255,255,255,0.05); } #crypto-ticker .ticker-name { font-size:clamp(0.8rem,4cqi,1.1rem); color:var(--fg,#ccc); min-width:5em; } #crypto-ticker .ticker-price { font-size:clamp(0.9rem,4.8cqi,1.3rem); font-weight:700; color:var(--fg-bright,#fff); font-variant-numeric:tabular-nums; } #crypto-ticker .ticker-change { font-size:clamp(0.7rem,3.6cqi,1rem); font-variant-numeric:tabular-nums; min-width:5em; text-align:right; }"
}